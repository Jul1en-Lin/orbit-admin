## Agent skills

### Issue tracker

议题以本地 Markdown 保存在 `docs/planning/`，不使用远程追踪系统。参见 `docs/agents/issue-tracker.md`。

### Triage labels

使用五个默认分诊标签字符串，记录为议题 front matter 的 `labels`。参见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文：仓库根目录 `CONTEXT.md` + `docs/adr/`。参见 `docs/agents/domain.md`。
